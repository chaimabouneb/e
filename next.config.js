/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["rb.gy", "images.unsplash.com", "unsplash.com", "i.ibb.co"],
  },
};

module.exports = nextConfig;
